apiVersion: apps/v1
kind: Deployment
metadata:
  name: incident-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: incident-app
  template:
    metadata:
      labels:
        app: incident-app
    spec:
      containers:
      - name: incident-app
        image: incident-app
        ports:
        - containerPort: 5000
